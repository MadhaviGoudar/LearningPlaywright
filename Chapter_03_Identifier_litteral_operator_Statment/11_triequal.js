// = === ==
let a=10;
console.log(a==10);
console.log(a === 10);  // its true because both value and type are same
console.log(a === "10"); // its false because value is same but type is different
console.log(a==10.0);// its true because value is same but type is different
console.log(a===10.0); // its true because both value and type are same
console.log(a==10.1);// its false because value is different